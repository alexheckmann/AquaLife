package aqua.client;

/**
 * Contains all possibilities which output channels should be recorded when creating a global snapshot
 */
public enum RecordingMode {
    IDLE,
    LEFT,
    RIGHT,
    BOTH
}
